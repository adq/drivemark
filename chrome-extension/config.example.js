// Copy this file to config.js and set your dev OAuth client ID.
// config.js is the dev config the extension imports directly (background.js).
// For production, publish.sh swaps config.prod.js in when packaging.
// See docs/chrome-extension-development.md.

export const CLIENT_ID = 'YOUR_WEB_CLIENT_ID.apps.googleusercontent.com';
